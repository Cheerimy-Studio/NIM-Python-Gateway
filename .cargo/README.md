# .cargo/ — Rust 编译配置

| 文件 | 作用 |
|---|---|
| `config.toml` | crates.io 依赖下载镜像加速（当前配置为 rsproxy.cn 国内镜像） |

## 说明

- 如果你在中国大陆网络环境，保留此配置可大幅加速依赖下载
- 如果你在海外或有自己的镜像偏好，可删除或修改 `config.toml` 中的 `[source]` 段
- 此配置**只影响本项目目录**，不会改动你的全局 Rust 设置
